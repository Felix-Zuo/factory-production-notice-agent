# Security

This repository is prepared as a public demo. Do not commit production BOM
files, customer lists, supplier records, generated notice archives, SQLite
indexes, executable launchers, or workstation logs.

The demo input in `sample_data/` is synthetic. Replace it only with masked
examples when adapting the project.
